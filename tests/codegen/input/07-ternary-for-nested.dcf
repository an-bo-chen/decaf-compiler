import printf;

void main(){
    int x;
    int y;

    for (x = 0; x < 3 ? true : false; x++){
        for (y = 0; y < 5 ? true : false; y += (x == 0) ? 2 : 1) {
            printf("%d, %d\n", x, y);
        }
    }
}