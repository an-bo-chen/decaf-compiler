import printf;

void main(){
    int x;
    int y;

    for (x = 0; x < 3; x++){
        y = 0;
        while(y < 3){
            if (y == 1){
                break;
            }
            printf("(%d, %d)\n", x, y);
            y += 1;
        }
        printf("row %d\n", x);
    }
}